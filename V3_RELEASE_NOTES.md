# Travel Camera v3.0.0

v2의 전체 화면 지도, 반투명 패널, 색상과 접히는 여행 설정을 계승하면서 v3의 재생 안정성·도움말·로컬 실행 기능과 사진 위치 판정 개선을 포함한 배포판입니다.

## 사용자에게 달라지는 점

- 설정 아래에 흩어진 설명을 마우스 올리기·키보드 포커스·? 버튼 도움말로 정리했습니다. 선택 옵션과 개별 체크 항목의 효과·적용 시점을 설명하며, 모바일에서는 탭으로 확인합니다.
- v3에서 새로 추가된 사진 여정·도움말·설정 UI도 v2의 색상 토큰을 따르도록 다시 맞췄습니다. 기본 잉크 `#171a1d`, 반투명 표면 `rgba(24,27,30,.92)`, 종이색 `#f7f3ed`, 보조 글자 `#aaa7a2`, 강조색 `#ff725d`를 기준으로 하며 사진 여정 배경도 v2의 `#181b1e`를 사용합니다.
- 화면 처리가 잠시 지연된 뒤 경로가 수초를 건너뛰던 문제를 수정했습니다. 멈춘 위치부터 자연스럽게 이어 보며 일시정지·재개도 불필요하게 탐색하지 않습니다.
- 기록이 끊긴 구간은 추정 연결 경로로 이어 재생합니다. 기록 없는 구간의 이동수단과 속도는 단정하지 않으며 별도 2초 정지를 추가하지 않습니다. 날짜 변경선 보간도 가까운 방향을 사용합니다.
- 경로의 GeoJSON 갱신은 최대 초당 30회로 제한하고, 사진을 감상하며 같은 위치에 머무르는 동안 중복 갱신을 생략합니다. 카메라 움직임은 브라우저 프레임에 맞춰 유지합니다.
- 지도 미리 읽기를 낮은 우선순위·동시 2개로 제한하며, 응답 본문 다운로드 완료까지 슬롯을 유지합니다. 4초 시간 제한과 지도 해제 시 취소로 불필요한 요청 누적을 방지합니다.
- 느린 초기 지도 요청을 5초 만에 중단하고 빈 배경으로 바꾸던 처리를 제거했습니다. 계속 로딩하면서 12초 이상 지연될 때 다시 연결 안내를 제공합니다. 지도 서버 연결도 미리 준비합니다.
- 사진 EXIF의 GPS 수평 오차 정보를 보존하고, 가까운 시간·위치의 연속 사진을 함께 고려해 한 장의 흔들린 GPS만으로 장소를 확정하지 않도록 개선했습니다.
- 비행기·기차·페리처럼 이동 중 촬영한 사진은 주변 POI를 억지로 확정하기보다 이동 상태를 우선하며, GPS와 Timeline이 크게 어긋나거나 정확도가 낮으면 상세 장소 판정을 보류합니다.
- 사용자가 선택적으로 켤 수 있는 정확한 장소 확인 기능을 추가했습니다. 호환 역지오코딩 서비스를 직접 설정한 경우에만 좌표를 조회하며, 결과는 `.cache/photo-places.json`에 저장해 같은 위치를 반복 전송하지 않습니다. 기본값은 OFF입니다.
- 로컬 PMTiles 지도의 글꼴 glyph도 최초 사용 후 `.cache/map-glyphs/`에 보관해 반복 실행 시 외부 의존을 줄였습니다.
- 지명은 가능한 경우 한국어를 우선하고, 한국어가 없으면 현지 기본명, 그다음 영어 순으로 표시합니다. 번역된 도시명 `파리`를 한국의 행정단위 `리`로 잘못 판단하는 경우도 방지했습니다.
- 개발·테스트 시 v2처럼 저장소 루트에 `타임라인.json`과 `여행 사진/`을 두면 별도 파일 선택 없이 자동으로 연결합니다. 루트에 없으면 기존 상위 폴더 위치를 확인하며 `TRAVEL_TIMELINE_PATH`·`TRAVEL_MEDIA_DIR` 지정은 항상 우선합니다. 두 루트 테스트 경로는 Git에서 제외됩니다.
- 배포 Worker 누락 수정, 전체 경로 보기, 설정 저장, 사진 목록, 영상 동기화, Windows 런처 등 이전 v3 개선을 유지합니다.

## 실행

ZIP을 풀고 Node.js 24 이상 환경에서 `npm ci` 후 `npm start`를 실행합니다. Windows에서는 `TravelCamera.cmd`를 실행할 수 있습니다. 기본 주소는 `http://127.0.0.1:5517`입니다. ZIP은 Git 저장소가 아니므로 자동 업데이트가 필요하면 Git 저장소에서 Windows 런처를 사용하는 편이 적합합니다.

`dist/`는 정적 호스팅용 배포 빌드입니다. 휴대폰에서 이용하려면 HTTPS 호스팅이 필요하며 PC의 loopback 주소로는 접속할 수 없습니다. 개인 Timeline·사진·로컬 지도 파일·캐시와 Git 이력은 배포 ZIP에 포함하지 않습니다.

정확한 장소 확인은 기본적으로 비활성화되어 있습니다. 사용하려면 Nominatim 호환 역지오코딩 서비스를 `TRAVEL_PLACE_REVERSE_URL`로 명시적으로 설정합니다. 공개 OSMF Nominatim은 개인 사진 좌표 보호를 위해 별도의 명시적 허용 없이 자동 사용하지 않습니다.

## 확인 범위와 한계

이번 갱신은 Node.js 24에서 타입 검사, 변경 관련 ESLint 및 단위 테스트를 통과했습니다. 로컬 가짜 역지오코더와 glyph 서버를 사용해 최초 온라인 결과 저장 후 공급자를 종료해도 장소 결과와 glyph가 로컬 캐시에서 재사용되는 것을 확인했습니다. 저장소 루트의 테스트 Timeline/사진 폴더 자동 연결과 루트가 없을 때 상위 폴더 fallback도 로컬 서버 API로 확인했습니다.

Chromium에서 1440×900 데스크톱과 Pixel 7 모바일 화면으로 시작 화면, 도움말, 사진 여정, 사진 설정, 사진 목록, 발자취, 발자취 설정을 다시 확인했습니다. v2 팔레트 토큰과 사진 여정 배경색도 데스크톱·모바일에서 다시 검증했으며 주요 겹침·가로 overflow·dialog 잘림은 발견되지 않았습니다. Windows·Android·iOS 실기기와 실제 개인 여행 파일의 전체 재생은 이번 갱신에서 별도로 수행하지 않았습니다.

공백과 마지막 전체 경로 전환은 재생 시간 안에서 진행합니다. 기기 지연이 있으면 실제 감상이 표시 시간보다 길어질 수 있습니다. 수동 탐색은 선택한 위치로 즉시 이동합니다.

`main`은 항상 최신 완성본을 유지하며, v3 릴리스 기준 브랜치 `codex/v3-release`도 이 v3 배포 시점에는 `main`과 동일한 커밋을 가리킵니다.

## 2026-09-15 사진 여정 검토 수정

짧은 기록 공백의 비행 오분류, AUTO 줌 경계 보정, 큰 공백 및 마지막 전체 경로의 추가 정지, 설정창과 재생바 중첩, 768/820px 분할 불일치, 낮은 가로 화면 잘림을 수정했습니다. 추천 여행은 선택 후 닫히는 native select, 설명은 이름 hover/focus/tap으로 제공합니다. UI 문구를 기능에 맞게 정리하고 번들 한국어 가변 글꼴 및 동일 SVG 계열의 이동수단 아이콘을 적용했습니다. 영상은 일시정지·탐색 위치를 맞추고 짧은 파일의 마지막 화면을 유지하며 첫 화면 모드도 프레임 디코딩을 기다립니다.

검증: 새 회귀 테스트와 기존 전체 verify/Chromium E2E를 실행 대상으로 추가했습니다. 실제 개인 사진·영상 파일 및 실기기 결과는 이 자동 검사에 포함되지 않습니다.
