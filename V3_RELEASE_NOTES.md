# v3 배포 후보 · 구현 및 확인 기록

기준: `main`의 `db06a38752c9c99125e8a1e4b1ab183c48af6535`에서 시작한 `codex/v3-release`.

기존 Worker·경로 계획·카메라·로컬 미디어 엔진을 유지하고 사용자 화면과 설정 구조를 재설계했습니다. 검증된 엔진을 처음부터 다시 만드는 것보다 기능을 보존하면서 사용성과 오류 복구를 개선하는 편이 적합하다고 판단했습니다. 이 문서는 실제 기기 인증이나 무결함 보증을 의미하지 않습니다.

## 적용한 화면과 기능

| 영역 | v3의 동작 |
| --- | --- |
| 시작 화면 | Timeline 준비 방법 → 기간 확인 → 재생 순서를 설명하고 주 작업을 한 버튼으로 제시 |
| 여행 설정 | 기간·카메라/지도·사진/영상 3개 영역, 각 항목 설명·권장값·적용 시점, 고급 설정 접기 |
| 감상 설정 저장 | 검증된 값만 브라우저에 저장, 기본값 복원, 개인 파일·날짜 저장 제외 |
| 사진 목록 | 촬영 시각 출처와 GPS/Timeline 추정 표시, 24개씩 목록, 감상 제외·모두 포함, 포함 항목에서 대표 장면 재선정 |
| PC·모바일 | 좌우/상하 지도·사진 분할, 작은 화면에서도 파일 선택·사진 목록 접근, 조절 가능한 분할선 |
| 접근성 | native dialog, 키보드 포커스·닫기, 설명 연결, 단축키, 큰 조작 영역, 모션 줄이기·고대비·safe-area |
| 오류 복구 | 처음 계획 실패 후 재시도, 기간 검증, 지도 재연결, 미디어 형식 안내, 렌더 오류 복구 화면 |
| 재생 | 모드별 위치 유지, 탭 숨김 시 일시정지, 영상 일시정지·탐색 동기화, 자동 재생 차단 시 수동 재생 |
| 배포 실행 | npm start와 Windows 런처는 프로덕션 화면 사용, 소스 변경 시에만 자동 빌드 |

## 함께 수정한 문제

- 첫 경로 생성 실패 후 계획이 없어 다시 만들기 버튼이 비활성화되는 문제.
- 지도를 바꾼 뒤 controller 교체로 커서가 초기화되거나 재생 표시와 실제 상태가 어긋나는 문제.
- 사진·날짜 stop 중 HUD 시간 갱신이 멈추는 문제와 영상 재생이 앱의 pause/seek를 따르지 않는 문제.
- 사진 전환의 이전 장면 제거 타이머가 HUD 텍스트 갱신에 의해 취소되는 문제.
- 날짜 경계를 넘는 이동 전체가 선택 기간에 포함되던 문제와 날짜 변경선 경로가 반대 방향으로 보간되던 문제.
- 로컬 사진 ID가 목록 순서에 의존하던 문제. 상대 경로·파일 크기·mtime fingerprint로 변경.
- 같은 사진의 반복 분석, 일시적인 Range 읽기 실패의 영구 캐싱, 촬영 시각 출처 오표시. 캐시 버전은 3으로 갱신하며 첫 실행에서 재분석할 수 있음.
- Windows npm.cmd 직접 spawn, 파일 stream 오류·연결 해제, 잘못된 빈 Range 처리.
- 상세 지도 범위 밖에서 세계 지도까지 사라질 수 있던 스타일 제한.

## 이번에 수행한 확인

사용자의 테스트 최소화 요청에 따라 범위를 제한했습니다.

| 확인 | 결과 |
| --- | --- |
| ESLint | 통과, 경고 0 |
| TypeScript + Vite 프로덕션 빌드 | 통과 |
| App·날짜/경도 경계 핵심 검사 | 10개 통과 |
| 사전 로드·이전 장면 유지·전환 안정성 기존 검사 | 선택한 3개 통과, 다른 9개 미실행 |
| npm start / 기존 빌드 재사용 | 프로덕션 서버 시작, 재빌드 없이 준비 |
| 로컬 서버 HEAD / | HTTP 200 |
| git diff --check | 통과 |

전체 단위 테스트·브라우저 E2E는 로컬에서 실행하지 않았습니다. 기존 E2E의 라벨·설정창 선택자는 v3 구조에 맞췄습니다. 실제 Windows, Android/iOS, 화면 크기별 시각 검사, 스크린리더 검사는 미실행입니다. PR의 자동 CI 결과는 위 로컬 확인과 별개입니다.

## 배포 판단과 남은 범위

핵심 사용 흐름, 설명, 오류 복구, 배포 실행을 갖춘 **v3 배포 후보**입니다. 실제 배포 확정 전에는 실제 여행 파일로 PC·모바일의 표시와 영상 조작을 한 번 확인해야 합니다. 이번 구현은 GitHub 브랜치/PR이며 main 병합·공개 호스팅·릴리스 태그 생성은 포함하지 않습니다.

- PC의 loopback 서버는 같은 PC 전용입니다. 모바일 제공은 dist의 HTTPS 정적 호스팅이 필요하며 그 경우 각 기기의 파일을 직접 선택합니다.
- 기본 온라인 지도와 일부 글꼴에는 인터넷이 필요합니다. 설치형 지도는 완전한 오프라인 모드를 의미하지 않습니다.
- HEIC/HEIF 및 일부 영상 코덱은 브라우저 지원에 따릅니다. 대용량·손상 파일 전체에 대한 검증을 수행한 것은 아닙니다.
- 날짜 표시는 한국 시간입니다. 여행지 시간대 선택은 다음 확장 후보입니다.
- 확장은 여행 프로젝트 저장/다시 열기 → 여행지 시간대 → 영상 내보내기 순서가 적합합니다. 파일 권한·미디어 인코딩·호스팅 정책을 수반하므로 이번 UI 개편에 임의로 섞지 않았습니다.
