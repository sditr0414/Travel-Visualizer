# Travel Camera v3.0.0

v2의 전체 화면 지도, 반투명 패널, 색상과 접히는 여행 설정을 계승하면서 v3의 재생 안정성·도움말·로컬 실행 기능과 사진 위치 판정 개선을 포함한 배포판입니다.

## 사용자에게 달라지는 점

- 설정 이름에 마우스를 올리거나 키보드로 이동하면 설명이 표시됩니다. 모바일에서는 이름을 눌러 설명을 열고 다시 누르거나 바깥을 눌러 닫습니다. 별도의 ? 버튼과 상단 안내 문구는 제거했습니다.
- v3에서 새로 추가된 사진 여정·도움말·설정 UI도 v2의 색상 토큰을 따르도록 다시 맞췄습니다. 기본 잉크 `#171a1d`, 반투명 표면 `rgba(24,27,30,.92)`, 종이색 `#f7f3ed`, 보조 글자 `#aaa7a2`, 강조색 `#ff725d`를 기준으로 하며 사진 여정 배경도 v2의 `#181b1e`를 사용합니다.
- 화면 처리가 잠시 지연된 뒤 경로가 수초를 건너뛰던 문제를 수정했습니다. 멈춘 위치부터 자연스럽게 이어 보며 일시정지·재개도 불필요하게 탐색하지 않습니다.
- 기록이 끊긴 구간은 추정 연결 경로로 이어 재생합니다. 기록 없는 구간의 이동수단과 속도는 단정하지 않으며 별도 2초 정지를 추가하지 않습니다. 날짜 변경선 보간도 가까운 방향을 사용합니다.
- 경로와 현재 위치의 GeoJSON을 하나의 source로 묶어 카메라와 같은 화면 프레임에 갱신합니다. 사진을 감상하며 같은 위치에 머무르는 동안 중복 갱신을 생략합니다.
- 지도 미리 읽기를 낮은 우선순위·동시 2개로 제한하며, 응답 본문 다운로드 완료까지 슬롯을 유지합니다. 4초 시간 제한과 지도 해제 시 취소로 불필요한 요청 누적을 방지합니다.
- 느린 초기 지도 요청을 5초 만에 중단하고 빈 배경으로 바꾸던 처리를 제거했습니다. 계속 로딩하면서 12초 이상 지연될 때 다시 연결 안내를 제공합니다.
- 사진 EXIF의 GPS 수평 오차 정보를 보존하고, 가까운 시간·위치의 연속 사진을 함께 고려해 한 장의 흔들린 GPS만으로 장소를 확정하지 않도록 개선했습니다.
- 비행기·기차·페리처럼 이동 중 촬영한 사진은 주변 POI를 억지로 확정하기보다 이동 상태를 우선하며, GPS와 Timeline이 크게 어긋나거나 정확도가 낮으면 상세 장소 판정을 보류합니다.
- 정확한 장소 확인은 호환 역지오코딩 서비스를 직접 설정한 경우에만 좌표를 조회하며, 결과는 `.cache/photo-places.json`에 저장합니다. 기본값은 OFF입니다.
- 로컬 PMTiles 지도의 글꼴 glyph는 최초 사용 후 `.cache/map-glyphs/`에 보관합니다. UI 글꼴과 지도 glyph의 로딩 방식은 서로 다릅니다.
- 지명은 가능한 경우 한국어를 우선하고, 한국어가 없으면 현지 기본명, 그다음 영어 순으로 표시합니다. 번역된 도시명 `파리`를 한국의 행정단위 `리`로 잘못 판단하는 경우도 방지했습니다.
- 저장소 루트에 `타임라인.json`과 `여행 사진/`을 두면 자동으로 연결합니다. 루트에 없으면 기존 상위 폴더 위치를 확인하며 `TRAVEL_TIMELINE_PATH`·`TRAVEL_MEDIA_DIR` 지정은 항상 우선합니다. 두 루트 테스트 경로는 Git에서 제외됩니다.
- 배포 Worker 누락 수정, 전체 경로 보기, 설정 저장, 사진 목록, 영상 동기화, Windows 런처 등 이전 v3 개선을 유지합니다.

## 실행

Node.js 24 이상 환경에서 `npm ci` 후 `npm start`를 실행합니다. Windows에서는 `TravelCamera.cmd`를 실행할 수 있습니다. 기본 주소는 `http://127.0.0.1:5517`입니다. Git 저장소의 `main`에서 추적 중인 변경이 없을 때 Windows 런처가 fast-forward 업데이트를 적용합니다. ZIP에는 Git 이력이 없으므로 자동 업데이트 기능이 없습니다.

`dist/`는 정적 호스팅용 배포 빌드입니다. 휴대폰에서 이용하려면 HTTPS 호스팅이 필요하며 PC의 loopback 주소로는 접속할 수 없습니다. 개인 Timeline·사진·로컬 지도 파일·캐시와 Git 이력은 배포 ZIP에 포함하지 않습니다. 이번 수정 작업에서 별도의 배포 ZIP이나 릴리스 태그는 발행하지 않았습니다.

정확한 장소 확인은 기본적으로 비활성화되어 있습니다. 사용하려면 Nominatim 호환 역지오코딩 서비스를 `TRAVEL_PLACE_REVERSE_URL`로 명시적으로 설정합니다. 공개 OSMF Nominatim은 개인 사진 좌표 보호를 위해 별도의 명시적 허용 없이 자동 사용하지 않습니다.

## 초기 v3 검증 기록

초기 v3 갱신에서는 Node.js 24의 타입 검사, 변경 관련 ESLint 및 단위 테스트를 통과했습니다. 당시 로컬 가짜 역지오코더와 glyph 서버로 최초 온라인 결과 저장 후 공급자를 종료해도 장소 결과와 glyph가 로컬 캐시에서 재사용되는 것을 확인했습니다. 루트의 테스트 Timeline/사진 폴더 자동 연결과 상위 폴더 fallback도 확인했습니다.

초기 화면 검증은 Chromium 1440×900과 Pixel 7 에뮬레이션을 사용했습니다. 이후 사진 여정 검토에서 추가로 발견한 공백·카메라·반응형 문제와 수정 범위는 아래 기록을 기준으로 합니다. 과거 검증 결과를 현재 모든 환경에서 문제가 없다는 보장으로 해석하지 않습니다.

## 2026-09-15 사진 여정 검토 수정

### 재생과 카메라

- 근거가 없는 짧은 좌표 공백을 최소 1초로 나눈 속도로 비행으로 분류하지 않습니다. 시간·경로 증거가 부족한 구간은 `visual-gap`, UNKNOWN, 속도 미확정으로 처리합니다.
- 50km 이상 공백도 재생 계획 안에서 연결합니다. 별도 알림으로 재생 시계를 멈추거나 진행선을 비우지 않습니다. 항공을 제외한 경우 해당 비행 선은 연결 선으로 다시 그리지 않습니다.
- AUTO의 추가 확대 보정은 비행 전후 0.85초 범위에서 연속적으로 변합니다. 기존 카메라 경로와 사용자 zoomOffset 1회 적용 원칙은 유지합니다.
- 마지막 전체 경로 전환도 표시된 재생 시간 안에서 진행하며 별도 2초를 추가하지 않습니다. 기기 지연이 있으면 실제 감상 시간은 길어질 수 있습니다. 수동 탐색은 선택 위치로 바로 이동합니다.

### 화면과 조작

- 설정창을 재생바보다 위에 표시하고, 열린 설정에서는 재생 버튼을 비활성화합니다. 파일을 선택하면 설정창을 자동으로 닫는 기존 흐름은 유지합니다.
- React와 CSS의 화면 분할 기준을 820px로 통일했습니다. 고정 최소 높이를 제거해 낮은 가로 창에서도 재생바와 자막이 화면 안에 배치되도록 했습니다.
- 추천 여행은 한 줄의 표준 선택 상자에서 고릅니다. 선택 후 후보 목록이 펼쳐진 채 남지 않습니다.
- 설정 이름의 도움말은 마우스·터치·키보드로 사용합니다. 첫 탭 전에 발생하는 hover/focus가 설명을 바로 닫는 문제를 수정했습니다.
- UI 글꼴은 앱에 포함된 Noto Sans KR Variable을 사용하며 외부 폰트 CDN을 호출하지 않습니다. 이동수단 픽토그램은 같은 Lucide SVG 계열로 통일했습니다.
- `경로 보기`, `지도 보기 방식`, `표시할 사진`, `대표 사진만`, `모든 사진`, `사진을 볼 때 지도 확대`, `경로 재생 시간`, `첫 화면만 표시`, `장소 정보 없음`으로 문구를 정리했습니다.

### 사진·영상 전환

이전 장면과 다음 장면이 겹치는 전환은 유지하되 취소된 프레임 예약이 장면 상태를 앞서 바꾸지 않도록 수정했습니다. 영상은 일시정지·탐색 위치를 맞추고 짧은 파일의 마지막 화면을 유지합니다. 첫 화면 모드도 메타데이터뿐 아니라 실제 프레임 디코딩을 기다립니다.

### 검증 범위

Node.js 24에서 타입 검사·ESLint·128개 단위 테스트·coverage·production build가 통과했습니다. Chromium에서는 1440×900, 390×844, 768×1024, 820×1180, 844×390의 재생바/지도 경계, 설정창 hit-test, 가로 넘침, 번들 글꼴 로딩을 검사했습니다. 데스크톱과 모바일 에뮬레이션에서 도움말, 추천 여행 선택, 모드별 독립 커서를 검증했습니다.

브라우저에서 생성한 WebM을 실제 디코딩해 재생·일시정지·뒤로 탐색·마지막 화면 유지도 데스크톱과 모바일에서 통과했습니다. 테스트 파일명 시각은 한국 시간으로 맞췄으며, 파일 선택 후 자동으로 닫힌 설정창을 테스트가 다시 여는 잘못된 조작을 바로잡았습니다. 최종 통합 검사 결과와 실행 로그는 PR #16의 `test` workflow와 `browser-regression-evidence` artifact를 기준으로 합니다.

현재 작업 환경의 로컬 실행 도구가 응답하지 않아 첨부 개인 타임라인의 재계산이나 원본 사진·영상의 전체 재생은 이번 수정 후 다시 실행하지 못했습니다. 개인 데이터는 CI나 저장소에 추가하지 않았습니다. Windows·Android·iOS 실기기 및 원본 MP4·오디오 검증은 자동 Chromium 검사와 구별합니다.

## 2026-09-15 사용자 피드백 후속 수정

재생/일시정지의 보이는 글자를 제거하고 접근성 이름은 유지했습니다. 이동수단을 이전 컬러 이모지로 복원하고 페리·비행기는 이모지 표시 선택자를 명시했습니다. 도보의 Accessibility 아이콘 매핑은 제거했습니다. 한글 UI 글꼴은 변경하지 않았습니다.

설정 설명의 좌표를 진입 애니메이션 중에도 이름의 실제 위치에 맞추고, 스크롤 밖의 이름이나 닫힌 설정의 설명이 남지 않도록 했습니다. 다른 이름을 가리키면 이전 고정 설명이 닫히며 입력 조작으로 설명이 다시 열리지 않게 했습니다.

카메라는 매 프레임인데 경로/현재 위치는 30Hz로 갱신되던 불일치를 제거했습니다. 두 지도 레이어는 같은 source의 선/점을 사용하므로 별도 Worker 메시지로 어긋나지 않습니다. 최근 4초 경로만 전송하고 사진 정지 중 같은 geometry는 재전송하지 않습니다. 지도 미리 읽기에서 같은 스타일을 source마다 직렬화하던 중복도 제거했습니다.

검증에는 60/120Hz 프레임 제출·선/점 일치·항공 제외·사진 정지, 이모지, 도움말 동작, 브라우저 좌표 및 렌더 제출 측정을 포함합니다. 실제 기기의 고정 60fps 보장과는 구별합니다. 원본 사진/영상이 없는 범위는 합성 미디어로 검증하며 개인 타임라인은 저장소나 CI에 넣지 않습니다.
