# Travel Camera v3.0.0

v2의 전체 화면 지도, 반투명 패널, 색상과 접히는 여행 설정을 계승한 배포판입니다.

## 사용자에게 달라지는 점

- 설정 아래에 흩어진 설명을 마우스 올리기·키보드 포커스·? 버튼 도움말로 정리했습니다. 선택 옵션과 개별 체크 항목의 효과·적용 시점을 설명하며, 모바일에서는 탭으로 확인합니다.
- 화면 처리가 잠시 지연된 뒤 경로가 수초를 건너뛰던 문제를 수정했습니다. 멈춘 위치부터 자연스럽게 이어 보며 일시정지·재개도 불필요하게 탐색하지 않습니다.
- 기록이 끊긴 구간 사이의 순간 이동을 약 2초의 카메라 전환으로 바꿨습니다. 해당 공백은 실제 경로 선으로 연결하지 않습니다. 날짜 변경선 보간도 가까운 방향을 사용합니다.
- 경로의 GeoJSON 갱신은 최대 초당 30회로 제한하고, 사진을 감상하며 같은 위치에 머무르는 동안 중복 갱신을 생략합니다. 카메라 움직임은 브라우저 프레임에 맞춰 유지합니다.
- 지도 미리 읽기를 낮은 우선순위·동시 2개로 제한하며, 응답 본문 다운로드 완료까지 슬롯을 유지합니다. 4초 시간 제한과 지도 해제 시 취소로 불필요한 요청 누적을 방지합니다.
- 느린 초기 지도 요청을 5초 만에 중단하고 빈 배경으로 바꾸던 처리를 제거했습니다. 계속 로딩하면서 12초 이상 지연될 때 다시 연결 안내를 제공합니다. 지도 서버 연결도 미리 준비합니다.
- 배포 Worker 누락 수정, 전체 경로 보기, 설정 저장, 사진 목록, 영상 동기화, Windows 런처 등 이전 v3 개선을 유지합니다.

## 실행

ZIP을 풀고 Node.js 24 이상 환경에서 `npm ci` 후 `npm start`를 실행합니다. Windows에서는 `TravelCamera.cmd`를 실행할 수 있습니다. 기본 주소는 `http://127.0.0.1:5517`입니다. ZIP은 Git 저장소가 아니므로 자동으로 main으로 바뀌지 않습니다.

`dist/`는 정적 호스팅용 배포 빌드입니다. 휴대폰에서 이용하려면 HTTPS 호스팅이 필요하며 PC의 loopback 주소로는 접속할 수 없습니다. 개인 Timeline·사진·로컬 지도 파일·캐시와 Git 이력은 배포 ZIP에 포함하지 않습니다.

## 확인 범위와 한계

사용자 요청에 따라 로컬 검사는 변경된 App·재생기·지도 미리 읽기의 핵심 회귀 검사와 타입 검사·린트·배포 빌드로 제한했습니다. 배포 미리보기에서 초기 지도 표시, 합성 경로, PC 및 390px 모바일 도움말 배치를 확인했습니다. Windows·Android·iOS 실기기와 실제 개인 여행 파일의 전체 재생은 확인하지 않았습니다. 외부 지도 응답 속도는 네트워크에 따라 달라집니다.

경로 시간에는 카메라 전환과 기기 지연이 포함되지 않으므로 실제 감상은 표시 시간보다 길어질 수 있습니다. 수동 탐색은 선택한 위치로 즉시 이동합니다.

릴리스 기준 브랜치는 `codex/v3-release`입니다. main 병합과 공개 웹 호스팅 배포는 별도입니다.
