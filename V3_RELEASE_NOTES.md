# v3 · v2 디자인 계승 및 지도 수정

최종 방향은 v2 디자인 유지입니다. 이전의 전면 재설계는 사용자 피드백에 따라 철회했습니다.

## 이번 수정

- v2 전체 화면 지도, 반투명 패널, 색상·타이포그래피·기존 배치와 접히는 여행 설정을 복원했습니다.
- 설정마다 효과와 적용 시점을 짧게 설명합니다. 도움말·사진 목록·감상 설정 저장은 기존 화면에 어울리게 유지합니다.
- 파일 선택 전부터 지도가 비던 원인: MapLibre 6의 기본 Worker 경로가 Vite 배포 청크 옆을 가리키지만 해당 파일이 빌드에 포함되지 않았습니다. Worker를 명시적으로 번들링해 지도 타일·GeoJSON 경로 처리를 복구했습니다.
- 존재하지 않는 정적 asset 요청은 HTML 대신 404로 응답합니다. 지도 로딩 오류를 빠뜨리지 않고 안내합니다.
- 전체 경로 버튼으로 재생 위치를 변경하지 않고 동선을 화면에 맞출 수 있습니다.
- 첫 계획 실패 재시도, 지도 교체 시 커서 유지, 영상 pause/seek 동기화, 날짜·경도 경계 처리, 미디어 캐시·Windows 런처 수정은 유지합니다.

## 확인 범위

사용자의 테스트 최소화 요청에 따라 로컬에서는 App 핵심 검사, 린트, 타입 검사·배포 빌드를 확인합니다. 배포 화면에서 한국·일본 배경 지도가 실제 표시되는 것을 확인했고, 390px 모바일 viewport에 가로 넘침이 없음을 확인했습니다. 기존 E2E의 첫 흐름에 지도 Worker 시작 확인을 추가했습니다.

이전 커밋의 CI 성공만으로 지도 렌더링을 보장할 수 없었습니다. 수정 커밋의 CI 결과는 PR에 별도 기록합니다. Windows·Android·iOS 실기기·스크린리더 검증은 수행하지 않았습니다.

현재 브랜치: `codex/v3-release`, PR: https://github.com/sditr0414/travel-camera-visualizer/pull/11. main 병합·공개 배포는 포함하지 않습니다. PC loopback 서버는 같은 PC 전용이고 모바일 제공에는 HTTPS 정적 호스팅이 필요합니다.
