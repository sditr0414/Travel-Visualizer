name: day-marker-default-2-5-check

on:
  push:
    branches:
      - ai/day-marker-default-2-5
    paths:
      - .github/workflows/day-marker-default-2-5-check.yml

permissions:
  contents: write

jobs:
  focused-check:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: python scripts/apply-day-marker-default-2-5.py
      - run: git diff --check
      - run: npm run typecheck
      - run: npx eslint src/App.tsx src/App.test.tsx --max-warnings=0
      - run: npx vitest run src/App.test.tsx
      - name: Commit focused changes
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add src/App.tsx src/App.test.tsx
          git commit -m "Set day marker default to 2.5 seconds [skip ci]"
          git push
# rerun after updating both photo journey duration expectations
